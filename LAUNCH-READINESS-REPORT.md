# گزارش آمادگی انتشار شریفمند — ۱۴۰۵/۰۵/۲۳

این سند نتیجه چرخه کامل «بازرسی → برنامه → پیاده‌سازی → تست → بازبینی» است. قانون صفر رعایت شد: **هیچ بازنویسی از ابتدا انجام نشد**؛ معماری موجود حفظ و اجزای کارآمد تقویت شدند.

## نمره کلی آمادگی: ۸٫۵ / ۱۰

| محور | نمره | توضیح کوتاه |
|---|---|---|
| درستی داده (بدون داده جعلی) | ۱۰/۱۰ | همه آمار، امتیاز، تایم‌لاین و لینک‌ها یا واقعی شدند یا حذف/صادقانه‌سازی شدند |
| جریان‌های کامل کاربر | ۹/۱۰ | «تعداد کم، کامل»؛ چند جریان فرعی هنوز لایه راهنما هستند |
| پرداخت | ۹/۱۰ | انتزاع ارائه‌دهنده، حالت دستی/سندباکس با بنر شفاف، بدون موفقیت ساختگی |
| کیفیت فنی (DoD) | ۱۰/۱۰ | typecheck ✓ lint(۰ خطا/۰ هشدار) ✓ build ✓ (۴۶ صفحه) |
| ناوبری و لینک‌ها | ۹/۱۰ | ناوبری داشبوردها زنده شد؛ تمام لینک‌های داخلی به next/link تبدیل شدند |
| اعتماد و صداقت محتوا | ۱۰/۱۰ | SLAها یکدست شد؛ تمام ابزارهای امتیاز/شبیه‌ساز با برچسب روش‌شناسی |

## کار انجام‌شده در این مرحله (PR #3)

**داشبوردها (P1)**
- صفحه جزئیات پرونده (`dashboard/cases/[id]`) که قبلاً کاملاً ساختگی بود (تایم‌لاین و وکیل هاردکدشده) با داده واقعی دیتابیس بازنویسی شد؛ کنترل دسترسی بر اساس مالکیت پرونده اعمال می‌شود و در نبود داده، وضعیت خالی صادقانه نمایش داده می‌شود.
- فهرست پرونده‌ها (`dashboard/cases`) ساخته شد؛ ردیف‌های پرونده در داشبورد موکل به جزئیات لینک می‌شوند.
- «اقدام بعدی شما» در داشبورد موکل: مرکز فرمان واقعی که از وضعیت پرونده/مشاوره کاربر مشتق می‌شود.
- داشبورد وکیل: آمار جعلی (۸۵٪ تکمیل پروفایل، نمودار درآمد ساختگی، «۷ درخواست جدید») حذف و با شمارش‌های واقعی (درخواست‌های در انتظار، نوبت‌های ثبت‌شده، جمع تسویه تأییدشده) جایگزین شد.
- ناوبری کناری داشبوردها که دکمه‌های «مرده» بود، با `href` به لینک واقعی تبدیل شد.
- `src/lib/case-facts.ts`: منبع واحد وضعیت/مرحله/اقدام‌بعدی — جلوگیری از انحراف آینده بین صفحات.

**رفع خرابی‌های واقعی**
- خطای نحوی در صفحه مشاوره (آبجکت `DURATIONS` به‌اشتباه با `];` بسته شده بود) — build را می‌شکست.
- صفحه «درباره ما» به ثابت حذف‌شده `STATS` ارجاع می‌داد؛ اکنون آمار واقعی دیتابیس با حالت تخریب امن (نمایش «—» به‌جای عدد جعلی).
- رفع خطای تایپ در ماژول نوبت‌دهی و بازنویسی پنل نوبت واقعی بدون خطای TDZ.
- فونت وزیرمتن local شد تا build به شبکه خارجی (Google Fonts) وابسته نماند.

**کیفیت و استاندارد**
- تمام لینک‌های داخلی `<a>` به `next/link` تبدیل شدند (۱۷+ فایل) → ناوبری SPA واقعی.
- الگوهای `setState-in-effect` با الگوهای رسمی React جایگزین شدند (مانند `useSyncExternalStore` برای hydration امن تم).
- خروجی نهایی: **typecheck پاک، lint بدون حتی یک هشدار، build موفق با ۴۶ صفحه**؛ چک Vercel سبز.

## آنچه در مراحل قبلی همین چرخه انجام شد (PR #1)
- سرویس‌های لایه‌بندی واقعی و صفحات خدمات، حذف داده نمایشی از صفحه اصلی و جستجوی وکلا
- QuikStart پنج‌مرحله‌ای با موتور توصیه واقعی، ادغام ورودهای AI، ریدایرکت مسیرهای تکراری
- انتزاع پرداخت (`lib/payments.ts`) با حالت تأیید دستی/سندباکس شفاف و بدون «موفقیت ساختگی»
- جریان مشاوره به «درخواست تا تأیید» اصلاح شد؛ SLA «کارشناسان تماس می‌گیرند» در رسید و تأییدیه تکرار شد
- صفحه کسب‌وکار با سطح‌بندی نتیجه‌محور و فرم «بررسی رایگان وضعیت کسب‌وکار»، ابزار مسیر ثبت برند با وکیل پایه‌یک

## بدهی‌های باقی‌مانده (مسیر به ۱۰/۱۰)
1. **درگاه بانکی واقعی**: متصل‌سازی PSP (زرین‌پال/زیبال) به انتزاع موجود — نیازمند کلید تجاری؛ رابط آماده است.
2. **اتصال حساب وکیل به پروفایل**: داشبورد وکیل فعلاً نمای عملیاتی پلتفرم است؛ پرونده/درآمد «شخصی» وکیل نیازمند مدل مالکیت است.
3. **اعلان و پیامک**: ارسال SMS/اعلان برای «تماس کارشناس» و تغییر وضعیت پرونده.
4. **تست E2E خودکار**: معیارهای فعلی (typecheck/lint/build + بازبینی دستی) سبزند؛ افزودن Playwright توصیه می‌شود.
5. **بارگذاری مدارک در جزئیات پرونده**: نمایش واقعی انجام شد؛ آپلود مستقیم از همان صفحه، گام بعدی.

## نتیجه
هیچ وعده‌ای در UI که قابلیت واقعی نداشته باشد باقی نمانده است؛ هیچ عدد/آمار/تایم‌لاین/موفقیت‌پرداخت ساختگی‌ای در صفحات کلیدی وجود ندارد. محصول برای عرضه محدود (Soft Launch) آماده است؛ پنج بدهی بالا، راه‌راه رسیدن به ۱۰/۱۰ را تشکیل می‌دهند.
